# DESCRIPTION: 
# Variables used during builds.

variables:
  buildConfiguration: 'Release' 
  azureSubscriptionEndpoint: 'LTHT PPM POC'
  azureContainerRegistry: 'lthtuksppmconfigcentreconreg.azurecr.io'
  composeLocation: 'build/docker/docker-compose.yaml'
