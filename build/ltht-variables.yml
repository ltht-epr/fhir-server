# DESCRIPTION:
# Variables used during builds.

variables:
  versionMajor: "0"
  versionMinor: "1"
  buildConfiguration: "Release"
  azureSubscriptionEndpoint: "LTHT FHIR (Non-Production)"
  azureContainerRegistry: "lthtuksfhirnonprodconreg.azurecr.io"
  composeLocation: "build/docker/docker-compose.yaml"
